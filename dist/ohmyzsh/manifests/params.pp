class ohmyzsh::params (
  $user_name = 'vagrant',
  $theme = 'steeef',
  $plugins = ['git', 'node', 'npm'],
  # "ll ='ls -lAh'", --> not working 
  $aliases = [
  						"l  ='ls -la",
  						],
  ){

}